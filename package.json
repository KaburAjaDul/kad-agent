{
  "name": "kad-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "start:dry-run": "tsx src/index.ts --dry-run",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json",
    "check": "npm run typecheck && npm test",
    "test:release": "vitest run tests/release-supply-chain.test.ts",
    "audit": "npm audit --audit-level=high",
    "migrate": "tsx src/scripts/migrate.ts",
    "seed": "tsx src/scripts/seed.ts",
    "db:init": "npm run migrate && npm run seed",
    "register-commands": "tsx src/scripts/register-commands.ts",
    "sync:staging": "tsx src/scripts/sync-public-agenda.ts",
    "sync:staging:dry-run": "tsx src/scripts/sync-public-agenda.ts --dry-run",
    "reconcile:discord-events": "tsx src/scripts/reconcile-discord-scheduled-events.ts",
    "reconcile:discord-events:dry-run": "tsx src/scripts/reconcile-discord-scheduled-events.ts --dry-run"
  },
  "dependencies": {
    "discord.js": "^14.27.0",
    "dotenv": "^16.6.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "tsx": "^4.23.12",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  }
}
